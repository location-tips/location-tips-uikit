import { Canvas, Meta, ColorPalette, ColorItem } from '@storybook/blocks';

import './brand/brand01.css';

import palette from './Palette';

<Meta title="Design System/Palette/Brand 01" />

{
document.getElementsByTagName("html")[0].dataset.brand = "brand01"
}

# Brand01 palette

Palette for brand01 uikit design system

<Canvas>
    <ColorPalette>
    {
    Object.entries(palette.brand01.palette).map((token, i) => {
        return(
            <ColorItem
                key={i}
                title={`${token[0]} color`}
                colors={
                    Object.entries(token[1]).map((value) => {
                        return `var(${value[1].cssVar})`
                    })
                }
            />
            )
        })
    }
    </ColorPalette>

</Canvas>
